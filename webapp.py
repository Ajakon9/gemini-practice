import streamlit as st

# タイトルを表示
st.title("🏥 超シンプル BMI計算アプリ")
st.write("身長と体重を入力すると、BMIを自動計算します。")

# 入力フォームを作る（サイドバーという左側のエリアに配置）
height = st.sidebar.number_input("身長 (cm)", min_value=100.0, max_value=250.0, value=170.0)
weight = st.sidebar.number_input("体重 (kg)", min_value=30.0, max_value=200.0, value=60.0)

# 計算ボタン
if st.button("計算する"):
    # BMIの計算式（身長をcmからmに直す）
    bmi = weight / ((height / 100) ** 2)
    
    # 結果の表示
    st.metric(label="あなたのBMI", value=f"{bmi:.2f}")
    
    # 判定ロジック
    if bmi < 18.5:
        st.warning("判定: やせ型です。しっかり栄養を摂りましょう。")
    elif 18.5 <= bmi < 25:
        st.success("判定: 普通体重です。素晴らしい！")
    else:
        st.error("判定: 肥満気味です。生活習慣を見直しましょう。")